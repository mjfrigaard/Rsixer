# Application Server

Top-level server function that initialises all R6 module servers and
wires reactive values between them.

## Usage

``` r
app_server(input, output, session)
```

## Arguments

- input, output, session:

  Standard Shiny server arguments.

## Value

Nothing (called for side-effects).
